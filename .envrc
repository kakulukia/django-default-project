use uv

# for the above to work, add thie following function to your direnv config at $HOME/.config/direnv/direnvrc
# use_uv() {
#     # create the venv if it doesn't exist
#     if [[ ! -d .venv ]]; then
#         uv venv --python "$PYTHON_VERSION"
#     fi

#     source .venv/bin/activate
# }
